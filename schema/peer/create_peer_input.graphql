input CreatePeerInput {
    clientMutationId: String
    serverId: ID!
    name: String!
    description: String
    publicKey: String!
    allowedIPs: [String!]!
    endpoint: String
    presharedKey: String
    persistentKeepalive: Int
    hooks: [PeerHookInput!]
}
