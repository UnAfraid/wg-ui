input UpdatePeerInput {
    clientMutationId: String
    id: ID!
    name: String
    description: String
    publicKey: String
    endpoint: String
    allowedIPs: [String!]
    presharedKey: String
    persistentKeepalive: Int
    hooks: [PeerHookInput!]
}
